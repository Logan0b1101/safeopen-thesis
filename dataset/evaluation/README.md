This evaluation dataset was manually curated to assess SafeOpen’s
ability to detect malicious documents before user interaction.

Malicious samples include documents with embedded scripts, macros,
polyglot structures, and MIME mismatches.

Benign samples consist of clean user documents without active content.
Ground truth labels were assigned based on static inspection and known
file behaviors.
